const add = (a,b) => a+b;

const sub = (a,b) => a-b;

const greet = (user) => `Goom Morning ${user}`;

const user = "Avinash";

export {add, sub, greet, user}

// here add, sub, greet and user are behave as keys and their values are a and b


// {

//     add: (a,b) => a + b;
//     sub: (a,b) => a - b;
//     user: "Avinash"
// }